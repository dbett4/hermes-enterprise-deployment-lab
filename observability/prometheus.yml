global:
  scrape_interval: 5s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: enterprise-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - enterprise-api:8080
